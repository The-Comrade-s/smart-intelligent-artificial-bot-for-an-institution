# Base URL of the deployed backend (no trailing slash, no /api/v1 suffix —
# that's appended automatically). In production this is your Railway service
# URL, e.g. https://cosib-backend.up.railway.app
VITE_API_BASE_URL=http://localhost:8000
